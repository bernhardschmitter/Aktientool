# Deployment V3.22+

Alle Hauptdateien hochladen/ersetzen. Die Ordner `.github/workflows` und `scripts` sind enthalten. Auf dem iPad können diese Ordner bei Bedarf unverändert im Repository bleiben, wenn sie bereits funktionieren.
